Dynamic Premium Formula:
```
P = C * Rb * Rm * Dt * (1 + Da) * (1 - Ds)

Where:
P = Final premium
C = Coverage amount
Rb = Base rate for tier
Rm = Risk multiplier
Dt = Duration factor
Da = Dynamic adjustment
Ds = Discount factor
```
